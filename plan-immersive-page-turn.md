# خطة تحسين حركة الوراق في Jarida Live

## الهدف

تحويل حركة تقليب صفحات **Jarida Live** من دوران طبقة المحتوى كاملة إلى تقليب ورقة محسوسة وقريبة من المرجع `https://jarida-immersive.vercel.app/`: الورقة تخرج من محور الوسط، تنحني أثناء العبور، يظهر وجهها الأمامي والخلفي، ويتحرك معها الظل والحافة والـcrease بشكل طبيعي. ستبقى الأخبار اليومية عبر RSS، والتعليقات، والمشاركة، والصوت، والتقليب التلقائي، والتصميم العربي القديم بدون تغيير وظيفي.

## القرار التقني

سأبقي التنفيذ خفيفاً داخل React وCSS، بدون مكتبة flipbook خارجية أو صور/فيديوهات كبيرة. سيتم اعتماد نموذج **ورقة مستقلة sheet-by-sheet**: طبقة ورق أمامية وخلفية، `perspective` و`transform-style: preserve-3d`، ومحور دوران من جهة الـspine. سيتم تبديل المحتوى عند منتصف الحركة فقط، ثم إبقاء الورقة حتى نهاية الدورة حتى لا يظهر أي snap.

| الجزء | القرار | السبب |
|---|---|---|
| سطح المكتب | ورقة واحدة تعبر من محور الوسط مع spread ثنائي الصفحات | يحاكي الجريدة المفتوحة ويمنع دوران spread كامل ككتلة واحدة |
| الهاتف والتابلت | ورقة واحدة بعرض الشاشة مع نفس المنطق ولكن عمق أقل | يحافظ على القراءة العمودية ويقلل كلفة الرسم |
| الحركة | CSS transforms وopacity وfilter فقط | أداء أفضل على GPU وتوافق مع الإيقاف العكسي |
| التفاعل | click، keyboard، swipe/touch، وتقليب تلقائي | الحفاظ على طرق الاستخدام الحالية مع إضافة إحساس الجر إذا كان المرجع يعتمد عليه |
| المحتوى | لا تغيير في API أو RSS أو schema | الطلب بصري بالأساس، وأي تغيير backend غير ضروري يرفع المخاطر |
| النشر | checkpoint قابل للمراجعة ثم تحقق من GitHub/Vercel حسب حالة الربط | عدم حذف تاريخ المستودع أو استعمال force-push |

## مراحل التنفيذ

### 1. معاينة المرجع وتثبيت معايير الحركة

سأعاين الصفحة المرجعية بصرياً وأسجل شكل الورقة قبل التقليب، أثناء وصولها إلى منتصف الـspine، وبعد استقرارها. سيتم تحديد اتجاه الدوران RTL، موضع محور الانحناء، مدة الحركة التقريبية، مقدار الـperspective، شكل الظل على الورقة وتحتها، وسلوك النقر والسحب على desktop وmobile. إذا تعذر الوصول إلى بعض تفاصيل المرجع، سأعتمد على المبدأ المرئي الواضح: ورقة فعلية بوجهين، لا دوران مسطح للمحتوى كله.

### 2. إعادة تنظيم طبقة الحركة في الواجهة

سيتم تعديل `client/src/pages/Home.tsx` ليصبح عندنا محتوى الصفحة الحالية في الطبقة الخلفية، وطبقة ورقة متحركة مستقلة تحمل snapshot للوجه الحالي والوجه التالي. سيتم الحفاظ على `currentPage` و`itemsPerPage` وdual-page/single-page، لكن إدارة الحركة ستفصل بين ثلاثة أزمنة: بداية الدوران، تبديل المحتوى عند المنتصف، ونهاية الدوران. سيتم منع النقرات المتكررة أثناء الدورة وإبقاء `aria-busy` وحالات الأزرار صحيحة.

سيتم تحديث `client/src/pages/pageTurnTiming.ts` ليكون المصدر الوحيد لمدة الحركة ونقطة المنتصف. إذا تطلب المرجع جرّاً تفاعلياً، ستضاف حالة pointer progress قابلة للعكس؛ وإذا كان المرجع يعتمد على click فقط، سيبقى السحب كتحسين اختياري لا يهدد الاستقرار.

### 3. تحسين CSS والواقعية البصرية

سيتم تعديل `client/src/index.css` لإضافة sheet geometry واضحة: محور دوران من جهة الوسط، front/back face، edge highlight، crease رفيع، ظل متحرك على الـspine، وظل عابر يتغير حسب اتجاه `next` أو `prev`. ستستخدم CSS custom properties للاتجاه والمسافة بدل تكرار keyframes قدر الإمكان. ستتم إزالة أي قواعد قديمة غير مربوطة فعلياً بـJSX، مع إبقاء قواعد `prefers-reduced-motion` وfallback بدون 3D.

ستكون الحركة ناعمة ولكن غير مبالغ فيها: بداية قصيرة، عبور واضح حول 90 درجة، ثم استقرار خفيف عند نهاية الورقة. لن يتم تحريك layout أو width أو height أثناء التقليب؛ الحركة ستعتمد على `transform` و`opacity` و`filter` فقط.

### 4. الحفاظ على وظائف الجريدة

لن تتغير بنية المقالات أو fallback الخاص بـRSS أو IDs الخاصة بالتعليقات. سيتم التأكد من أن أزرار WhatsApp وFacebook وX ونسخ الرابط، وحقول التعليقات، وزر التحديث، وأزرار التحكم في الصوت والتقليب التلقائي لا تطلق تقليباً بالخطأ عند النقر عليها. سيبقى سطح المكتب بصفحتين والهاتف بصفحة واحدة، مع احترام RTL والـkeyboard و`prefers-reduced-motion`.

### 5. الاختبار والتحقق

سيتم إضافة أو تحديث اختبار timing يثبت أن تبديل المحتوى يحدث في منتصف الدورة وأن class الحركة لا تزال مفعلة حتى النهاية. ثم ستشغل الفحوصات التالية:

| الاختبار | معيار النجاح |
|---|---|
| TypeScript | `pnpm check` بدون أخطاء |
| Unit/Vitest | اختبارات timing والحركة وباقي الاختبارات الموجودة ناجحة |
| Production build | `pnpm build` يخرج bundle صالحاً بدون أخطاء |
| Desktop | spread بصفحتين، دوران من الوسط، ظل وحافة واضحان، ولا يوجد layout jump |
| Mobile/tablet | صفحة واحدة، swipe/click يعملان، لا يوجد overflow أو قص للمحتوى |
| Interaction | next/prev، auto-flip، الصوت، keyboard، والتعليقات/المشاركة لا تتكسر |
| Visual evidence | لقطات before/during/after أو تسجيل قصير يبين الورقة في منتصف الانحناء ثم استقرارها |
| Production | الصفحة الحية تعرض Jarida Live، وAPI وassets يرجعون بنجاح، ولا توجد صفحة بيضاء أو JS خام |

### 6. الحفظ والنشر

بعد نجاح الاختبارات، سيتم حفظ checkpoint وصفي في المشروع. إذا كان المطلوب تسليم Production أيضاً، سيتم دفع التغيير إلى GitHub على الفرع الصحيح، انتظار GitHub Actions/Vercel، ثم مطابقة commit المصدر مع رابط Vercel الحي. لن يتم استعمال force-push، ولن يتم تعديل إعدادات RSS أو قاعدة البيانات إلا إذا ظهر سبب ضروري أثناء التنفيذ.

## الافتراضات والمخاطر

أفترض أن المقصود هو تقليد **إحساس حركة الورق** في المرجع، وليس نسخ كوده أو أصوله. الخطر الأساسي هو أن CSS 3D قد يختلف قليلاً بين Chromium وSafari على الأجهزة الضعيفة؛ لذلك سأضيف reduced-motion وfallback مستقرين، وأتحقق من desktop وmobile بشكل منفصل. كما أن وجود مقالات طويلة وتعليقات داخل الورقة قد يجعل الورق يبدو ثقيلاً، لذلك سيتم الحفاظ على ارتفاع الحاوية ومنع إعادة التدفق أثناء الحركة.

إذا اتضح أثناء المعاينة أن المرجع يعتمد على سحب مستمر لا على click فقط، فسيكون ذلك قراراً توسعياً واضحاً: نضيف pointer capture وprogress متدرجاً، مع إبقاء click/auto-flip كمسار fallback. أما إذا كان المطلوب فقط تقليباً تلقائياً واقعياً، فسيتم تجنب تعقيد gesture والاكتفاء بورقة 3D مستقلة عالية الجودة.
